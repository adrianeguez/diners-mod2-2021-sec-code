// 01-variables.js
console.log('Hola mundo');

// Declarar variables

// Mutables -> = -> reasignadas

var variableUno = 1;
let variableDos = 2;
variableUno = 3;
variableDos = 3;

// Inmutables -> no = -> no reasignadas

const noPuedoCambiar = 1;
noPuedoCambiar = "212";